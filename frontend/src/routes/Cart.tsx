/**
 * TODO: not built yet.
 *
 * This file is a placeholder so the router resolves. Replace it wholesale -
 * it is owned by exactly one agent and shares no file with any other route.
 */
export default function Cart() {
  return (
    <section className="placeholder">
      <h1>Cart</h1>
      <p>Not built yet.</p>
    </section>
  );
}
